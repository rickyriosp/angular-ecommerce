// This file can be replaces during build by using the `fileReplacements` array.
// `ng build --configuration=qa or npm start -- --configuration=qa` replaces `environment.ts` with `environment.qa.ts`.
// The list of file replacements can be found in `angular.json`.
export const environment = {
  production: false,
  shopApiUrl: 'https://localhost:9898/api',
};
