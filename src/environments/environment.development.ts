// This file can be replaces during build by using the `fileReplacements` array.
// `ng build --configuration=development or npm start -- --configuration=development` replaces `environment.ts` with `environment.development.ts`.
// The list of file replacements can be found in `angular.json`.
export const environment = {
  production: false,
  shopApiUrl: 'https://localhost:8443/api',
};
